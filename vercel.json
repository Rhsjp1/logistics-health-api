{
  "version": 2,
  "builds": [
    { "src": "api/*.js", "use": "@vercel/node" },
    { "src": "*.html", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*\\.html)", "dest": "/$1" }
  ]
}
```

6. Click **Source Control** (left sidebar) → type `update vercel config` → **Commit & Push**

Then wait ~60 seconds for Vercel to redeploy and test:
```
https://logistics-health-api-9mmt.vercel.app/dashboard.html
